spring:
  application:
    name: discovery-service
  config:
    import: optional:configserver:http://localhost:8080

  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 5
        multiplier: 1.5

server:
  port: 8761
