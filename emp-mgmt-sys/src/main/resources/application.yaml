spring:
  application:
    name: emp-mgmt-sys
  config:
    import: optional:configserver:http://localhost:8080
  cloud:
    config:
      fail-fast: true
  profiles:
    active: dev

springdoc:
  swagger-ui:
    operations-sorter: alpha
    tags-sorter: alpha